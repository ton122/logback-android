language: android
android:
  components:
    - android-7
  licenses:
    - '.*'
before_install:
  - echo no | android create avd --force -n test -t android-7
  - emulator -avd test -no-skin -no-audio -no-window &
before_script:
  - ./ci/wait_for_emulator.sh
